// The large-project layout owns the overview's booking list. Keep this route
// intentionally empty so the overview contains bookings — and nothing else.
const LargeProjectViewPage = () => null;

export default LargeProjectViewPage;
